---

interface Props {
    /**
     * A string property.
     */
    foo: string;
    /**
     * A number property.
     */
    bar: number;
    /**
     * Example Template literal
     */
    tempLit: `${string}-${string}-${string}.${number}`
}
---
