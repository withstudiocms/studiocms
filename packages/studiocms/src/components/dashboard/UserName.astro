---
import type { UserSessionData } from 'studiocms:auth/lib/types';

interface Props {
	currentUser: UserSessionData;
}

const { currentUser } = Astro.props as Props;
---
<Fragment set:text={currentUser.user?.name} />