/**
 * API Handlers Index - Exports all API handlers for the StudioCMS API routes.
 *
 * This file serves as the central export point for all API handlers related to the StudioCMS API. It currently includes the SDK API handler, but can be extended in the future to include additional handlers for other API specifications or functionalities.
 *
 * Note: Some handlers such as the SDK handler may not be included in this bundle as they can be required when the normal API routes are not available (e.g., such as during the first time setup process).
 */
export const APILiveHandlers = void 0; // Layer.mergeAll(API1, OtherAPILiveHandlers);
